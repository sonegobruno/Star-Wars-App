export class Film {
    title: string;
    episode_id: number; 
    producer: string;
    opening_crawl: string;
    release_date: string;
    director: string; 
    characters: string[]; 
    planets: [ ]; 
    starships: [ ]; 
    vehicles: []; 
    species: []; 
    url: string;
}